var React = require("react");

var TodoApp = React.createClass({
  render: function() {
    return (
      <div>
        Space for rent
      </div>
    );
  }
});

module.exports = TodoApp;